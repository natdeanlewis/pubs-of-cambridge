import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pubsofcambridge.vercel.app',
  appName: 'Pubs of Cambridge',
  webDir: 'dist'
};

export default config;
